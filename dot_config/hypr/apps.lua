-- Personal app-specific window rules.

-- g15 control panel TUI (launched from the waybar module), float like the
-- other omarchy TUIs (wifi/audio).
o.window("org.omarchy.g15-tui", { tag = "+floating-window" })
